# 项目协作规则

## 定位

本项目是**提示词与方法的工具箱**，不是个人思考记录仓库。入库的是可复用的提示词、方法论、证据标准与模板；个人的分析记录、对话原文、洞察卡片留在本地，不入库。

## 内容规则

- 只用通用表述：不放个人经历、真实姓名、学校、单位、家庭情况、联系方式等可识别信息。
- 不引用私人对话原文；需要说明设计动机时，写成"常见失效模式"这类通用描述。
- 事实类内容（数据、机构名、来源链接）必须可核查；给不出来源就不写，或明确标注"未核实"。
- 提示词中不出现具体案例的结论性判断（例如对某个时事下定性），保持工具的中立性。
- 术语统一：`事实 / 推论 / 价值`、`L1 / L2 / L3`、`来源分级 A/B/C/D`。

## 提示词编写规则

- `prompts/*.md` 是**纯提示词**，整文件可直接复制粘贴：不写使用说明、不加分隔标题、不嵌套代码块。
- 使用说明、定位、迭代记录写在 `README.md` 与 `docs/`。
- 占位符统一用 **【…】**，位置固定在正文靠前部分。
- 每个模块必须包含：输出结构、硬性禁止清单、收尾动作（追问 + 边界声明）。
- 纪律类要求（三分法、禁止项、收尾格式）在 `00-master.md` 与各模块中保持一致；改一处必须同步检查其余文件。

## 迭代规则

- 只在真实问题跑过之后修改，并说明改动的理由与预期效果。
- 如果一次改动让输出变长但判断没变，回滚该改动。
- 已发布的远端是公开仓库，推送前确认内容不含个人信息。

## 操作边界

- 本地可自由提交；创建远端、推送、改可见性需明确授权。
- 不把本地洞察卡片、分析记录复制进仓库目录。

## 自检清单

提交前逐项确认：

- [ ] 全文没有个人可识别信息
- [ ] `prompts/` 下每个文件都是可直接复制的纯提示词
- [ ] 每个模块都有禁止清单与收尾追问
- [ ] 引用的事实、链接、数字可核查，或已标注未核实
- [ ] 纪律类要求与 `00-master.md` 一致
- [ ] `README.md` 的导航表与文件实际路径一致
